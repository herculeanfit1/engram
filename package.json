{
  "name": "engram",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "main": "index.js",
  "scripts": {
    "start": "op run --env-file=.env.1p.template -- node index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "vitest": "^4.1.4"
  }
}
